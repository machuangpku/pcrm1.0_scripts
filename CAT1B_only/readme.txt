把avs-pcc-decoder.exe和avs-pcc-encoder复制到目录下
cfg使用yaml生成的版本
只需要修改config.py的数据集路径
0.py为主要运行文件，把0.py复制几个就有几个线程
复制的文件命名为1.py，2.py。内容不用修改
根据自己需求在config.py的all_config里面修改需要的线程的配置
#每个线程不同的配置，参数表示为
#条件[0,4]，运行的数据集[0,9],
#C1运行的条件[0,6]，有损有损
#C2运行的条件[0,6]，无损有损
#C3运行的条件[0,5]，无损有限损
#C3运行的条件[0,1]，无损无损
#每行是一个线程
#目前默认设置是9线程，0.py-8.py
readall_anchor 读取左边表格，读取anchor
readall_test 读取右边表格，读取测试